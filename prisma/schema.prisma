datasource db {
  provider = "mongodb"
}

generator client {
  provider = "prisma-client-js"
}

model Trip {
  id                String @id @default(auto()) @map("_id") @db.ObjectId
  TotalParticipants Int
  Destination       String
  Menu              String
  Cost              Float
}